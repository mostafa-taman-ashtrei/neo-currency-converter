export interface currencyData {
    amount: string;
    to: string;
    from: string;
}

export interface historyType {
    date: Date;
    amount: number;
    result: number;
    to: string;
    from: string;
}
